# The Node version lives in .node-version, which CI reads too, so the number
# is written down once. mise ignores that file unless the tool is listed here.
# pnpm's version lives in package.json, where the workflows already read it.
[settings]
idiomatic_version_file_enable_tools = ["node"]
